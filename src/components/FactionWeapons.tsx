import React from 'react';

/**
 * @deprecated Use `<FactionReference />`, `<FactionWarbandLimit />`, `<FactionSpecialRules />`,
 *   `<FactionFighters />`, and the equipment components from `@site/src/components/wiki/FactionReference`.
 * Stub kept so old imports do not crash; renders nothing.
 */
export default function FactionWeapons(_props: Record<string, unknown>) {
  return null;
}
